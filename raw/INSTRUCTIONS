1. Run scripts/initial_process.sh PREFIX

    Generates 'taxa' and '%.loci.head' files

2. Run scripts/split_loci.sh PREFIX

    Generates per-locus MSA files in 'perlocus' directory

3. Run scripts/gather_info.sh PREFIX

    Generates loci.desc file

4. Generate taxa info for all defined (or selected) filters

    $ scripts/stat_info.sh PREFIX [filter]

5. Generate process files for all defined (or selected) filters

    $ scripts/create_proc.sh PREFIX [filter]

6. Build the concatenated MSA

    $ scripts/build_supermatrix.sh PREFIX

    Generates 2 files: supermatrix.[phy|fas] and supermatrix.parts
    If you want to generate several according to different filters, do not 
    forget to backup the previous ones!
